# teal.fm live viewer

watch live fm.teal.alpha.feed.play scrobbles stream in from the jetstream.

## to run

1. install dependencies

   ```bash
   npm install
   ```

2. start the app

   ```bash
   npx expo start
   ```
